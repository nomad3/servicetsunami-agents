# Local development (Vite dev server proxies /api to this)
VITE_API_BASE_URL=http://localhost:8000

# Production (set in CI pipeline for Tauri builds)
# VITE_API_BASE_URL=https://agentprovision.com

# Docker PWA (empty = relative paths, nginx proxies /api internally)
# VITE_API_BASE_URL=
