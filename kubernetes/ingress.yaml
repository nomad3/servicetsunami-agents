# BackendConfig for API health check
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: servicetsunami-api-backend
  namespace: prod
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /api/v1/
---
# Combined Ingress for ServiceTsunami
# Routes traffic to both API and Web services using a single Load Balancer IP
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: servicetsunami-ingress
  namespace: prod
  annotations:
    kubernetes.io/ingress.class: gce
    networking.gke.io/managed-certificates: servicetsunami-cert,agentprovision-cert,healthpets-cert
  labels:
    app.kubernetes.io/name: servicetsunami
    app.kubernetes.io/component: ingress
spec:
  rules:
    # Main domain - Web frontend serves root, API serves /api
    - host: "servicetsunami.com"
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: servicetsunami-api
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: servicetsunami-web
                port:
                  number: 80
    # www subdomain redirects to main (handled by web frontend)
    - host: "www.servicetsunami.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: servicetsunami-web
                port:
                  number: 80
    # agentprovision.com - development/alternate domain
    - host: "agentprovision.com"
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: servicetsunami-api
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: servicetsunami-web
                port:
                  number: 80
    - host: "www.agentprovision.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: servicetsunami-web
                port:
                  number: 80
    # Health Pets - veterinary cardiac monitoring platform
    - host: "pets.agentprovision.com"
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: healthpets-backend
                port:
                  number: 80
          - path: /uploads
            pathType: Prefix
            backend:
              service:
                name: healthpets-backend
                port:
                  number: 80
          - path: /ws
            pathType: Prefix
            backend:
              service:
                name: healthpets-backend
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthpets-frontend
                port:
                  number: 80
---
# Managed Certificate for HTTPS
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: servicetsunami-cert
  namespace: prod
  labels:
    app.kubernetes.io/name: servicetsunami
    app.kubernetes.io/component: certificate
spec:
  domains:
    - servicetsunami.com
    - www.servicetsunami.com
---
# BackendConfig for Health Pets API health check + 300s timeout for agent calls
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: healthpets-backend-config
  namespace: prod
spec:
  timeoutSec: 300
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /health
---
# Managed Certificate for pets.agentprovision.com
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: healthpets-cert
  namespace: prod
  labels:
    app.kubernetes.io/name: healthpets
    app.kubernetes.io/component: certificate
spec:
  domains:
    - pets.agentprovision.com
