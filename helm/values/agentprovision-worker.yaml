# AgentProvision Background Worker - Temporal Worker for Background Jobs
nameOverride: "agentprovision-worker"
fullnameOverride: "agentprovision-worker"

image:
  repository: gcr.io/ai-agency-479516/agentprovision-api
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 1

container:
  port: 8000
  command:
    - python
    - -m
    - app.workers.worker

# Service account with Workload Identity
serviceAccount:
  create: true
  annotations:
    iam.gke.io/gcp-service-account: dev-backend-app@ai-agency-479516.iam.gserviceaccount.com

# Pod security
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

# Resource allocation - minimal for cost savings
resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

# Disable HTTP probes - worker doesn't serve HTTP
livenessProbe:
  enabled: false

readinessProbe:
  enabled: false

# Service not needed for worker
service:
  type: ClusterIP
  port: 80
  targetPort: 8000

# ConfigMap with non-sensitive configuration
configMap:
  enabled: true
  data:
    TEMPORAL_NAMESPACE: "default"
    DEFAULT_WORKFLOW_TIMEOUT_SECONDS: "600"
    DATA_STORAGE_PATH: "/app/storage"

# External Secrets - same as API service
externalSecret:
  enabled: true
  refreshInterval: 1m
  secretStoreRef:
    name: gcpsm-secret-store
    kind: SecretStore
  target:
    name: agentprovision-worker-secret
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: agentprovision-database-url
    - secretKey: MCP_API_KEY
      remoteRef:
        key: agentprovision-mcp-api-key
    - secretKey: GOOGLE_API_KEY
      remoteRef:
        key: agentprovision-google-api-key
    - secretKey: API_INTERNAL_KEY
      remoteRef:
        key: agentprovision-api-internal-key

# Additional environment variables
env:
  - name: TEMPORAL_ADDRESS
    value: "temporal:7233"
  # See orchestration-worker.yaml for the rationale on why both URLs
  # point at agentprovision-mcp (the live FastMCP service) and the
  # deprecated mcp-server:8000 reference is gone.
  - name: MCP_SERVER_URL
    value: "http://agentprovision-mcp:80"
  - name: MCP_TOOLS_URL
    value: "http://agentprovision-mcp:80"
  - name: API_BASE_URL
    value: "http://agentprovision-api"

# No autoscaling - single instance
autoscaling:
  enabled: false

# Persistent storage for datasets (shared with API)
persistence:
  enabled: true
  storageClass: standard-rwo
  accessModes:
    - ReadWriteOnce
  size: 50Gi

# Temporary directories
tmpDirs:
  - /tmp

# Init container to wait for Temporal
initContainers:
  - name: wait-for-temporal
    image: busybox:1.36
    command: ['sh', '-c', 'echo "Waiting for Temporal..."; sleep 15']
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

# Cloud SQL Proxy sidecar for secure database connection
extraContainers:
  - name: cloud-sql-proxy
    image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.8.0
    args:
      - "--private-ip"
      - "--structured-logs"
      - "--port=5432"
      - "--http-port=9090"
      - "ai-agency-479516:us-central1:dev-postgres-instance"
    securityContext:
      runAsNonRoot: true
      allowPrivilegeEscalation: false
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 100m
        memory: 128Mi

# No HTTPRoute - internal service only
httpRoute:
  enabled: false
