name: Luna Client — Tauri Build

on:
  push:
    branches: [main]
    paths:
      - 'apps/luna-client/**'
      - '.github/workflows/luna-client-build.yaml'
    tags:
      - 'luna-v*'
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: self-hosted
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install frontend dependencies
        working-directory: apps/luna-client
        run: npm ci

      - name: Install Rust (if needed)
        run: |
          source "$HOME/.cargo/env" 2>/dev/null || true
          if ! command -v cargo &> /dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          fi
          rustc --version
          cargo --version

      - name: Install Tauri CLI
        run: |
          source "$HOME/.cargo/env" 2>/dev/null || true
          cargo install tauri-cli --version "^2" 2>/dev/null || true

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/luna-v* ]]; then
            VERSION=$(echo "${{ github.ref_name }}" | sed 's/luna-v//')
          else
            # Auto-increment: MAJOR.MINOR from tauri.conf.json, PATCH = commits since last luna-v* tag
            BASE=$(python3 -c "import json; print(json.load(open('apps/luna-client/src-tauri/tauri.conf.json'))['version'])")
            MAJOR=$(echo "$BASE" | cut -d. -f1)
            MINOR=$(echo "$BASE" | cut -d. -f2)
            # Count commits since last luna-v* tag (or all commits if no tag exists)
            LAST_TAG=$(git describe --tags --match "luna-v*" --abbrev=0 2>/dev/null || echo "")
            if [ -n "$LAST_TAG" ]; then
              PATCH=$(git rev-list "${LAST_TAG}..HEAD" --count)
            else
              PATCH=$(git rev-list HEAD --count -- apps/luna-client/)
            fi
            VERSION="${MAJOR}.${MINOR}.${PATCH}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build Tauri (macOS ARM64)
        working-directory: apps/luna-client
        env:
          VITE_API_BASE_URL: https://agentprovision.com
        run: |
          source "$HOME/.cargo/env" 2>/dev/null || true
          cargo tauri build --target aarch64-apple-darwin

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: luna-macos-arm64
          path: |
            apps/luna-client/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/Luna.app
            apps/luna-client/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/Luna_*.dmg
          retention-days: 30

      - name: Generate updater manifest
        working-directory: apps/luna-client
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          DMG_PATH=$(ls src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/Luna_*.dmg)
          DMG_NAME=$(basename "$DMG_PATH")
          TAG="luna-v${VERSION}"

          SIG=""
          if [ -n "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}" ]; then
            source "$HOME/.cargo/env" 2>/dev/null || true
            SIG=$(cargo tauri signer sign "$DMG_PATH" --private-key "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}" 2>/dev/null | tail -1 || echo "")
          fi

          cat > latest.json <<EOF
          {
            "version": "$VERSION",
            "notes": "Luna $VERSION",
            "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "platforms": {
              "darwin-aarch64": {
                "signature": "$SIG",
                "url": "https://github.com/nomad3/servicetsunami-agents/releases/download/${TAG}/${DMG_NAME}"
              }
            }
          }
          EOF

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        working-directory: apps/luna-client
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          TAG="luna-v${VERSION}"
          DMG_PATH=$(ls src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/Luna_*.dmg)

          # Create tag if it doesn't exist
          git tag "$TAG" 2>/dev/null || true
          git push origin "$TAG" 2>/dev/null || true

          # Create release (or update if exists)
          gh release create "$TAG" \
            --title "Luna $VERSION" \
            --generate-notes \
            "$DMG_PATH" \
            latest.json \
            --latest 2>/dev/null || \
          gh release upload "$TAG" \
            "$DMG_PATH" \
            latest.json \
            --clobber 2>/dev/null || true
